log("=== Test Pattern ===")

let cost_report = {"recommendations": ["rec1", "rec2"]}
let recommendations = cost_report["recommendations"]
if recommendations {
    let i = 0
    while i < 4 && i < len(recommendations) {
        log("  * " + recommendations[i])
        i = i + 1
    }
}

log("")
log("=== Complete ===")
log("Review the findings.")