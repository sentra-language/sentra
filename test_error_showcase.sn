// Comprehensive runtime error testing showcase
log("=== Sentra Runtime Error Handling Demo ===")

// Test 1: Division by zero
log("\n1. Testing division by zero...")
fn test_division() {
    log("About to divide by zero...")
    let result = 10 / 0
    log("This line should never execute")
}

// Test 2: Array bounds checking  
log("\n2. Testing array bounds...")
fn test_array_bounds() {
    let arr = [1, 2, 3, 4, 5]
    log("Array length: " + str(len(arr)))
    log("Accessing valid index arr[2]: " + str(arr[2]))
    log("About to access invalid index arr[10]...")
    let invalid = arr[10]
    log("This line should never execute")
}

// Test 3: Map key not found
log("\n3. Testing map key errors...")
fn test_map_keys() {
    let user = {"name": "Alice", "age": 30, "role": "admin"}
    log("Valid access user['name']: " + user["name"])
    log("About to access missing key user['salary']...")
    let salary = user["salary"]
    log("This line should never execute")
}

// Execute tests (comment/uncomment to test different errors)
log("\nRunning division by zero test...")
test_division()

// Uncomment these to test other errors:
// log("\nRunning array bounds test...")
// test_array_bounds()

// log("\nRunning map key test...")
// test_map_keys()

log("Demo completed successfully!")