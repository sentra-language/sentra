// Test for-in loop over arrays first

log("Testing for-in loop over arrays...")

let test_array = ["a", "b", "c"]

log("Array contents:")
for item in test_array {
    log("  " + item)
}

log("Array test completed")