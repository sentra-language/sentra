log("Testing tree structure...")

let tree = mem_get_process_tree()
log("Tree type: " + type(tree))

if (tree) {
    log("Total: " + str(tree["total"]))
    log("Roots: " + str(tree["roots"]))
    
    log("Tree field type: " + type(tree["tree"]))
    
    if (tree["tree"]) {
        let tree_data = tree["tree"]
        log("Tree data type: " + type(tree_data))
        log("Tree data length: " + str(len(tree_data)))
    }
}

log("Done!")