#!/usr/bin/env sentra

log("Test array indexing in nested if")

let arr = [1, 2, 3]
if (arr) {
    log("Array exists")
    if (len(arr) > 0) {
        log("Array has elements")
        let first = arr[0]
        log("First element: " + str(first))
    }
}

log("Test complete")