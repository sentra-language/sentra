// Test for-in loop over maps to reproduce stack overflow

log("Testing for-in loop over maps...")

let test_map = {
    "key1": "value1", 
    "key2": "value2",
    "key3": "value3"
}

log("Map contents:")
for item in test_map {
    log("  " + item + ": " + test_map[item])
}

log("Test completed")