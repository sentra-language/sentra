log("Testing mem_enum_processes...")

let processes = mem_enum_processes()
log("Type: " + type(processes))
log("Total: " + str(len(processes)))

for (let i = 0; i < len(processes); i = i + 1) {
    let proc = processes[i]
    log(str(i) + ": PID " + str(proc["pid"]) + " - " + proc["name"])
}

log("Done!")