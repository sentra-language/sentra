#!/usr/bin/env sentra

log("Testing parser fixes")

// Test escape sequences in nested conditionals
let data = {"key": "value"}
if (data) {
    log("\n✓ Data exists")
    if (data["key"]) {
        log("\t✓ Key found: " + data["key"])
    }
}

// Test special characters
log("\n📊 Special characters test")
log("Tab:\tIndented")
log("Newline:\nNew line here")

// Test complex nested structure
let results = [{"name": "test", "value": 123}]
if (results) {
    if (len(results) > 0) {
        let first = results[0]
        if (first) {
            log("Name: " + first["name"])
        }
    }
}

log("\n✅ All tests passed!")